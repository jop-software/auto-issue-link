name: "PR Issue Link"

on:
  pull_request:
    types: [opened]

jobs:
  link-pr-to-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: 'actions/checkout@v4'
      - name: Run action from local path
        uses: ./
